<div class="panel panel-default">
  <!-- Panel Title -->
  <div class="panel-heading">
    <div class="row">
      <div class="col-md-1">
        <h3 class="panel-title">Profiles</h3>
      </div>
      <div class="col-md-2 col-md-offset-9">
        <button class="btn btn-xs btn-primary pull-right" type="button" name="button" ng-click="add()" data-toggle="modal" data-target="#editProfile">
          Add Profile
        </button>

      </div>
    </div>
  </div>

  <!-- Panel Body -->
  <div class="panel-body">

    <div class="" ng-if="!profiles || profiles.length == 0">
      <li class="list-group-item">
        <div class="no-data-large">
          <span class="glyphicon glyphicon-list-alt"></span> <br/><br/>
          <span class="lead">Add Profiles in the Fiesta</span>
          <span class="text-muted">
            <p>No activities added in the fiesta. <a href ng-click="add()" data-toggle="modal" data-target="#editProfile">Add one</a>?</p>
          </span><br/>
          <br/>
        </div>
      </li>
    </div>

    <ul class="list-group blog-holder" ng-repeat="profile in profiles">
      <li class="list-group-item blog">
        <table style="width:100%; height:100%">
          <tr>
            <td class="col-md-4 blog-img-header text-right"
              ng-click="focus(profile)" data-toggle="modal" data-target="#viewProfile"
              ng-style="{'background-image':'url(' + profile.image.path + ')'}">
              <span ng-if="(profile.image && profile.image.credits && profile.image.credits.trim() != '')" class="small blog-img-header-credits">&copy; {{profile.image.credits}}</span>
            </td>
            <td class="col-md-6 text-left blog-info">
              <h3>
                <a href style="color:black;" ng-click="focus(profile)" data-toggle="modal" data-target="#viewProfile">{{profile.title}}</a>
              </h3>
              <span class="small text-muted text-capitalize" ng-if="profile.author.trim() != ''">
                By &nbsp; {{profile.author}}
              </span>
              <br>
              <span class="small text-muted">
                <strong>
                  {{profile.timestamp | date:'MMMM dd, yyyy'}}
                </strong>
              </span>

            </td>
            <td class="col-md-1 text-right">
              <table style="height:100%">
                <tr>
                  <td>
                    <a href ng-click="select(profile)" data-toggle="modal" data-target="#editProfile" tooltip-placement="left" uib-tooltip="Edit Details">
                      <span class="glyphicon glyphicon-edit"></span>
                    </a>
                  </td>
                </tr>
                <tr>
                  <td>
                    <a href ng-click="point(profile)" data-toggle="modal" data-target="#editImage" tooltip-placement="left" uib-tooltip="Change Image">
                      <span class="glyphicon glyphicon-camera"></span>
                    </a>
                  </td>
                </tr>
                <tr>
                  <td>
                    <a href ng-click="DeleteProfile(profile._id)" tooltip-placement="left" uib-tooltip="Delete">
                      <span class="glyphicon glyphicon-trash"></span>
                    </a>
                  </td>
                </tr>
              </table>
            </td>
          </tr>
        </table>
      </li>
    </ul>

  </div>
</div>

<edit-profile></edit-profile>
<view-profile></view-profile>
